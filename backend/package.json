{
  "name": "lofi-space-station-backend",
  "type": "module",
  "private": true,
  "version": "1.0.0",
  "description": "NASA ISS stream analyzer & WebSocket status broadcaster",
  "main": "server.js",
  "scripts": {
    "start": "node --env-file=.env.local server.js",
    "dev": "nodemon --env-file=.env.local server.js"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "sharp": "^0.34.3",
    "ws": "^8.18.0"
  },
  "devDependencies": {
    "@types/node": "^25.2.0",
    "nodemon": "^3.1.11"
  }
}